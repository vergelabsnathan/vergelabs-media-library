{
  "phase": "Every picture a home — S22: the listing, then what was deferred. 4.0.0 is cut: the changelog, the Upgrade Notice and the rewritten External services section are written, the version is set in all three places, and Plugin Check on a clean archive gives no errors and two non-blocking warnings. Nothing mechanical is left. What is waiting is Nathan's read of the copy (the proof page lists the three decisions), and after the form is sent: the licence key in the support ticket, the growing-shop case, the Uncategorized guard, and whatever his pass over the Folders shot sheet turns up.",
  "model": "opus",
  "plan": "a plan file for the growing-shop case before it is built — it is a new automatic write path on a real library and wants the six fields per task",
  "spec": "docs/superpowers/specs/2026-09-16-folders-at-catalogue-scale.md (S10.8, the order of the steps by library)",
  "scope": [
    "readme.txt (Nathan's copy edits, then re-cut and re-check)",
    "core/get-help.php (the licence key in the support ticket, if Nathan says so)",
    "core/auto-file.php, core/filing.php (the growing shop: a product's picture placed on upload)",
    "core/guide.php vergeml_folders_product_paths (the Uncategorized guard)",
    "js/vergeml-folders.js, tests/**, docs/superpowers/mocks/** (a mock before any visible change)",
    "docs/**, the archive",
    "docs/handoffs/**",
    "plans/**"
  ],
  "readFirst": [
    "docs/handoffs/2026-09-19-s21-the-estimate-and-the-small-things.md (this session: the estimate, the four small things, the audit, and 4.0.0)",
    "docs/outbound-audit-2026-09-19.md (every outbound call and what it sends) and docs/release-inventory-2026-09-19.md (the 98 changes behind the changelog)",
    "docs/wordpress-org-submission.md (what is done and what the form needs)",
    "memory: verge-media-library-fork, do-the-cli-work, model-spend-discipline, progress-always-visible, ui-less-text-pills"
  ],
  "handoffDir": "docs/handoffs",
  "stopPoints": [
    "Every user-facing string is Nathan's, verbatim; a mock before any visible change",
    "The readme's External services section is a statement about what leaves a customer's site — never widen what the plugin sends without saying so there in the same commit",
    "The price of the model call is Nathan's; until priced it is metered, never debited",
    "The model never overrules a product placement, a hand placement, or files into a view or a locked folder",
    "Say every cost before it is spent: a describe is a credit a picture",
    "The wordpress.org form is Nathan's to send",
    "Test first, one mutation per story; a mutation of anything that can start a run holds the cron wire",
    "Never git checkout a file carrying uncommitted work; never chain a checkout into a command",
    "box-eval --site shop is the C.5 main site; --site realshop is the WooCommerce shop"
  ],
  "gates": [
    "node tools/box-eval.mjs tools/box-truth-score.php --site shop and the tech line with --copy tests/tree/truth-tech.json:/tmp/vgml-truth.json --env VGML_TRUTH=/tmp/vgml-truth.json (MSYS_NO_PATHCONV=1): both rules-only lines in every check-in (shop 347 of 581 (60 %) · 72 %, tech 153 of 200 (77 %) · 76 %) — neither falls",
    "node tools/verify.mjs filing sticky guide ai surface roles escaping copy tree-view talk-chips folders-shop seed-shop ai-background filing-trail → green (escaping 9/10 known); service pnpm test green",
    "node tools/deploy.mjs --check before trusting any box result: it names both digests, and a STALE box means the suite is testing yesterday",
    "folders.spec and modes.spec on the tech site after any screen change — all green at S21's end",
    "any readme change re-cut and re-checked: git archive, Playground, node tools/plugin-check.mjs — no errors, and the two known warnings only",
    "any change to what the plugin sends re-audited against docs/outbound-audit-2026-09-19.md and written into readme.txt in the same commit"
  ]
}
