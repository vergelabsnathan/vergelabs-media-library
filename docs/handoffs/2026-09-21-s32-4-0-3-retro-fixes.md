# S32 — the 4.0.3 story: Screen Options over the panel, the live tick skips aloud, the outage FAQ says what the code does

**Date:** 2026-09-21, evening. **Model:** Opus 5. **From:**
`docs/handoffs/2026-09-21-s31-wave-6-review-and-retros.md`. **Spec:**
`_bmad-output/implementation-artifacts/spec-4-0-3-retro-fixes.md` (status
`in-review`: the code is built, reviewed and proven; four copy decisions are
yours, below). Plugin `c293f7f` → `d38bcd3` (+ this handoff's commit);
service `33063ea` → `5b8ffa6` (`credits.md`, on `main`, unpushed behind the
pricing commits). **Spent: nothing.** Nothing on ms2 or the real shop; `upg`
back as found (`vergeml-tree.css` at `366f083fe3b0`, checked twice); the box
at HEAD (`deploy.mjs --check`: up to date); no version bump, no changelog
line, no push.

## What shipped (in the tree)

- **E4-1** `css/vergeml-tree.css`: `#screen-meta` at `z-index: 4`, over the
  list-mode panel's 3. On `upg` (free 4.0.2) the probe reproduced the retro
  exactly — **1/4** checkboxes reachable (Author → `button.vgml-fold`, Media
  Categories → `span.vgml-title`, Used on → `button.vgml-upload`) — and with
  the tree's sheet **4/4**, the folder row below the open panel and after it
  closes still taking the press. Shot
  `docs/superpowers/mocks/shots/2026-09-21-screen-options-over-tree-upg.png`.
  modes.spec reads every column checkbox at its centre and names what it hit:
  on the box **`10/10 checkboxes reachable`** in both modes, **2 passed**.
  The two A4 comments corrected. Tool: `tools/upg-screen-options-probe.mjs`
  (`--css <file>` copies, probes, restores with the digest; `--restore`).
- **E4-3** `test.skip( ! theirs.length, … )` after the tab-open and checkbox
  assertions. Exercised on Playground (`tools/play.mjs`): **2 skipped** with
  the reason, `4/4 checkboxes reachable` printed first. Getting there found
  the retro's edge-case 6 for real: set two expected the `-40` class from the
  box's geometry and stopped the row test on any other site — now `40` for
  five of ours, `0` for two (Playground), nothing between.
- **E3-2** the paragraph (yours, approved twice) in `readme.txt` and
  `service/docs/manual/credits.md`; `grep -c "described when it is back"` → 0
  in both. Behind it, **`tests/ai/outage.php` (`ai-outage`, Playground,
  32/32)**: unreachable → marked on the first miss; 503 → held, held, marked
  on the third; 400 → marked first time; marked pictures out of `unindexed`,
  in `missing-alt`; the Alt text pass with the service back clears the marks
  and writes the alt. Red under the E3-1 mutation (transport error made
  transient → **14/25** on the first version). Found by it: a held picture is
  reported *twice* in the step's `errors` (`ai.php:1541` and `:1584`).

## The pre-mortem earned its keep

The paragraph you approved first said "after four the run stops". False: the
step breaks, but the screen's loop and the background tick start the next
step and the run waits on the held pictures. Dropped before it shipped.

## Review: 24 rows, 12 patched, 5 deferred, 3 rejected, 4 yours

Four lenses over the diff (`spec-4-0-3-…md` "Review Triage Log"). The
patches: the suite refuses unless the backlog is exactly its own pictures
(it took the site's lowest id first — on the box it would have marked a real
picture), the 400 and the way-back steps, the probe's kept-original guards.
Deferred to E3-1 with `deferred-work.md` rows: the double report; the
parallel path (`vergeml_ai_transport`) has no seam and no test, so the
"cannot be reached" sentence is backed for the path no customer runs; **a
stub during a Re-describe run is merged onto the described row and
`error <> ''` drops it from search, filing and the counts** (real, confirmed
at `ai-index.php:390`); three carried-over clauses without a line; the
held-picture tick.

## Yours — four copy decisions (both documents; say go, or give the words)

- **D1** "When the service answers with an error" → *"When the service
  answers with a temporary error (a rate limit or a 5xx)"*. Only
  0/408/425/429/5xx are held; a 4xx or a 200 without a caption marks the
  picture on the first answer (suite step 5).
- **D2** "holds the picture and tries it again ten minutes later" → *"sets
  the picture aside for ten minutes and tries it again on its next pass"*. A
  run you are watching ends when only set-aside pictures are left; a
  background run keeps going; the next press reaches them.
- **D3** `service/docs/runbooks/incident.md:279-282` — the "ai relay"
  incident template still says "Pictures waiting to be described are
  described when it is back". Proposal: the paragraph's first three sentences
  in its place now.
- **D4** "everything already described keeps working" is false during a
  Re-describe run in an outage (above). Proposal: leave it; E3-1 makes it
  true.

**The 4.0.3 changelog line (yours):** *"Screen Options opens over the
folder panel on the media list, not under it; the AI-outage answer in the
FAQ says what a describe run does."* The 4.0.2 line "Screen Options and
Help open again" was half of it.

## Found, not done

- No repo wrapper makes the throwaway UI administrator around a local
  Playwright run (S28's note stands); mine is a scratch file. One small tool
  would save the next session the setup.
- The modes.spec set label reads "all 2 of ours on" on Playground — honest,
  odd; the count is the site's.

## Next — S33

1. **E3-1** as its own story (spec → build → review): transport errors
   transient with a seam around `request_multiple`; a stub never overwrites a
   described row; a marked picture with alt text has a way back; the double
   report; then the FAQ shrinks again. `tests/ai/outage.php` is its red test.
2. D1–D4 above, then the train for 4.0.3 (bump, changelog, cut) on your word.
3. **4.3's mock** (E4-5).

Opener:

```
Read docs/handoffs/2026-09-21-s32-4-0-3-retro-fixes.md, then
_bmad-output/implementation-artifacts/epic-3-retro-2026-09-21.md (E3-1) and
the five S32 rows at the end of deferred-work.md. State which model you are
and follow that profile in ~/.claude/harness/model-profiles.md. This session
is S33, story E3-1: bmad-spec (one question per step, pre-mortem), then
bmad-build with tests/ai/outage.php as the red test and a parallel-path
step, then bmad-code-review. Stop points: nothing on ms2 or the real shop;
every user-facing string is mine; say what goes out before any push; no
cut, no deploy. End with a handoff in docs/handoffs/.
```
