# S25 — Story 2.1: the buyer walk on 4.0.0, walked live

**Date:** 2026-09-20. **Model:** Opus 5. **From:**
`docs/handoffs/2026-09-20-s24-epic-1-retro-chores.md`. **Spec:**
`_bmad-output/implementation-artifacts/spec-2-1-the-buyer-walk-on-4-0-0.md`
(status `review`; sprint status: epic 2 `in-progress`, 2.1 `review`).
Plugin `e106e20` → `9a9ebe0` (spec) → `acf769b` (tools, shots) → this
handoff's commit; service `9da6ddd` → `f581f56` (pushed, serving); pro
untouched. **Spent: €0.50 on Nathan's card, 1 credit, one describe.**
Nothing on ms2 or the real shop; `upg` back as found.

## The score lines

Not re-taken: no engine, screen or filing code changed. S21's stand: shop
347 of 581 (60 %) · 72 %; tech 153 of 200 (77 %) · 76 %.

## What happened

- **Spec first** (`bmad-spec`, one question per step, then a pre-mortem
  via Advanced Elicitation, seven changes applied). Nathan's four calls:
  full price; a fresh `nathan+buyer-0920@vergelabs.nl`; the site is
  `upg`; cancel at period end afterwards. Then, at the Pay button: **not
  €39, €0.50** — `WALK0920` (€38.50 off `single`, one use) made through
  the store's own `createDiscountCode` (`service/scripts/make-walk-code.ts`,
  env pulled to a scratch file and deleted after), because PRIVATE0's €0.39
  is under Stripe's floor.
- **The walk, every step with its own artefact** — the full table is
  `service/docs/buyer-walk-2026-09-20.md`; shots
  `docs/superpowers/mocks/shots/2026-09-20-buyer-walk-01…14.png`:
  checkout **Pay €0.50** → `/order` "Your One site licence is ready", key
  shown → Stripe `pi_3UHjdrENqcXgcnrD1aETBxDM` succeeded, invoice
  **AWTXBCFC-0016** paid €0.50 (coupon `once`, tax `not_supported`),
  `sub_1UHjdqENqcXgcnrDW0hWTxmm` active to 2027-09-20 → licence email
  delivered → `verify single / 2000 / 0 sites` → registration + confirmation
  → `/account` **Download Pro 1.0.2 (zip)** = `2a6a7946426f…` → `upg`
  `1.0.2 active` → Licence screen **Connected 1 / 1, 2,000** → **Describe
  with AI** on picture 23: a real alt and caption, **We wrote this**,
  `1999` → `/account` **CREDITS LEFT 1,999**, `−1 Images described` →
  Billing `AWTXBCFC-0016 · paid €0.50`, our PDF Total €0.50, same number →
  "Cancelled. Everything keeps working until the end of the period you have
  paid for." → restore: seat `0/1`, key absent, Pro inactive, the picture's
  three rows **0 differences**, 0 Pro lines in `debug.log`.
- **Tools that stay:** `tools/buyer-walk.mjs` (headed window, persistent
  profile outside the repo; `buy --code … [--dry]`, `final`) and
  `tools/buyer-walk-site.mjs` (`install | connect | describe | restore`);
  `service/scripts/walk-inspect.mjs` (a payment's Stripe objects, read-only).
- **Fixed on Nathan's order, same session (service `f581f56`):** after
  "Create account" the form stayed with a notice line — as the buyer he
  said it doesn't feel right. Now the heading, lead and form give way to
  "Check your inbox — We sent a confirmation link to <address>. It works
  for 48 hours." with "Use a different address" back. Proven locally with
  the register request mocked (`Create your account` → `Check your inbox`,
  form gone, PASS), pushed, marker found live in
  `/_next/static/immutable/chunks/01mj1pjej7fcc.js`. Copy is my proposal;
  Nathan did not object — his to change.

## Found, not done (in `deferred-work.md`)

1. **A sub-minimum percent code is a free year.** PRIVATE0 on `single`
   (€0.39) — Stripe marked invoice AWTXBCFC-0012 `paid` at `amount_paid 0`,
   activated the subscription, the webhook issued licence `…AB26` with 2,000
   credits for nothing. The intent route needs a floor for subscriptions;
   PRIVATE0 is Nathan's to switch off or restrict to credits.
2. Billing lists abandoned checkouts as `open` invoices ("7 on file", five
   never confirmed).
3. The order page's "Go to your account" lands on Sign in for an address
   with no account (from the code, not observed).
4. Two licences on the walk account: `…93RJ` (paid, cancels 2027-09-20)
   and `…AB26` (free, active) — cancelling the second is one click, no money.
5. Three incomplete subscriptions from the abandoned checkouts expire on
   their own in Stripe.

## Open, and Nathan's

- `bmad-code-review` on 2.1 (the spec is at `review`); then epic 2's
  retrospective — 2.2 was recorded, 2.3 is the accountant's.
- The PRIVATE0 decision (found 1) and the `…AB26` click (found 4).
- Still yours from S22–S24: the 4.0.1 cut, Pro 1.0.3 and the minimum-free
  gate, the wordpress.org form, S-1, P-2, P-3, P-4; `7f2a4fe9bee9` on the shelf.
- One transport note: Playwright resolves only from `plugin/` — a service-side
  browser probe runs from there (or gets copied in and removed).

## Addendum, same day — the mixed mode

After the walk Nathan asked what finishing the suite would look like in a
decentralised build (agents in worktrees, one consolidation) instead of one
story per session, and said go. The answer is **`plans/finish-the-suite.md`**:
BMAD and the harness unchanged; six code-only stories (A FR10 key out of
the ticket, B FR12 CSV cells with a `csv-local` wasm leg, C the €0.50
floor, D Billing/order, E Pro's minimum-free gate, F the 4.0.1 copy
proposals) as Opus-shape mini-specs run in parallel worktrees, one
consolidation session on Fable 5.1, then the release train, the trimmed
matrix, the walk again, one decision sitting, the retros. Three to four
calendar days from the go on wave 1; about one working day of Nathan's.
Nothing has run yet.

## Addendum 2 — the launch smoke and story C, same day

- **Launch smoke, nothing spent:** GitHub `releases/latest` = `v4.0.0`,
  asset digest `bf0d63b70056…` = the served zip (200, 1,101,616 bytes);
  the release zip on a fresh WP 7.1.1 / PHP 8.3 Playground activates and
  all nine plugin screens answer 200 with their heading (shots
  `2026-09-20-launch-release-*.png`, `bdf739a`); the same 4.0.0 on `upg`
  renders seven admin screens (`shots.mjs`); site pages, all 20 docs pages,
  `/api/health`, the Pro update channel — 200 with markers. Not smoked: a
  support ticket send (a real ticket). Verdict: nothing technical blocks
  launching today; PRIVATE0 off and the wordpress.org form are Nathan's.
- **Story C done in one go** (`spec-c-subscription-floor.md`, service
  `7c12bf1`, serving): `eligible()` refuses a code that would put a
  recurring plan's first invoice under Stripe's floor (€0.50 / $0.50 /
  £0.30, zero included); test red → green, mutation caught, 513 passed
  overall; live `PRIVATE0` on `single` → `minimum_amount`. Found on the
  way: subscription purchases never write a redemption, so `max_uses` does
  not hold on yearly plans (`deferred-work.md`).
- Trap met twice today: a Bash heredoc followed by another command can hang
  the tool; one job per call.

## Next — S26

Wave 1 minus C (A, B, D, E, F). Wave 0 is Nathan's (PRIVATE0 off; `…AB26` cancelled). Then wave 1: six
sessions with the per-agent opener in the plan, or "use a workflow" in one
session to orchestrate them. The plan is the thing to read first, and to
elicit on A and C before their agents start.
