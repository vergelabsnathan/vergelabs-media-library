# S23 — Pro 1.0.2 works on 4.0.0 (story 1.3), Epic 1's last story

**Date:** 2026-09-20. **Model:** Opus 5. **From:**
`docs/handoffs/2026-09-19-s22-bmad-first-run-and-the-channel-on-4-0-0.md`.
**Spec:** `_bmad-output/implementation-artifacts/spec-1-3-pro-1-0-2-works-on-4-0-0.md`
(status `done`; sprint status: story 1.3 `done`, **`epic-1: done`**). Plugin
`5ead7d4` → `374ab5e` (+ this handoff's own commit); pro `a57bc77` →
`ef1709b`. Nothing spent; nothing on
ms2 or the real shop; every activation of the seats key was followed by its
release in the same run (12 activations, 12 releases, the last `0/1`).

## The score lines

Not re-taken: no engine, screen or suite of the free plugin changed. S21's
stand: shop 347 of 581 (60 %) · 72 %; tech 153 of 200 (77 %) · 76 %.

## What was built

**`pro/tests/compat-free.php`** — the four touch points of AD-6 through Pro's
own hooks, with the free 4.0.0 archive active and the Pro 1.0.2 archive
(`2a6a7946426f`, the `0825e17` build customers have) activated on top:

- Activation: not in safe mode, the basename `VGMLPRO_REQUIRES`, not ready
  without a licence.
- Licence: the seats key activates against the live service (`plan single,
  1 / 1`), `vgmlpro_ready()`, `vgmlpro_load_features()` loads the describe
  path; the seat is handed back as a check before the summary line.
- Index: a row through `vergeml_index_set()` reads back with `alt, model,
  locked, error, described_at`, and `vgmlpro_provenance_from_index()` reads
  it as AI-written, unedited.
- Profile: the brief saved through the free plugin's own route callback
  (`vergeml_ai_rest_settings`, also `mock => 1`) is what Pro's
  `vgmlpro_describe_attachment()` sends as `profile` — the service stood in
  for by a `pre_http_request` filter that keeps the request, on a one-pixel
  probe attachment removed afterwards.
- Column: `manage_media_columns` carries `vgmlpro_source` with Pro's label,
  `vergeml_list_our_columns()` names it, it is off the default set (as
  3.16.1), the cell renders "None yet" for an unknown picture.
- Every warning, notice or deprecation raised from a Pro file during the run
  is collected and asserted absent.

Exits 2 (SKIPPED in the free runner) without a key or with Pro inactive; a
Pro that is active but in safe mode is a FAIL. Restores key, state and
`vergeml_ai` exactly as found (absent → deleted).

**`pro/tools/verify.mjs`** — an archives leg: a suite marked `archives: true`
unzips `../dist`'s two zips (sha256 pinned `bf0d63b70056` / `2a6a7946426f`)
and mounts those instead of the working trees, `pro/tests` beside them;
`--tree` overrides it. **`plugin/tools/verify.mjs`** — `compat-free-upg`, the
same file on `/var/www/upg`. **`plugin/tools/upg-pro-shots.mjs`** — the three
screens as `vgmls22`: activates Pro if it finds it inactive, connects the test
licence, sets the column option a customer's Screen Options would, judges
each screen on what it shows, releases the seat, puts everything back, and
exits 1 unless every step said so itself.

## The evidence

- Playground on the archives: **`23/23 passed`**, `deactivating gives the seat
  back -- sites_used 1 -> 0 (1 taken), valid yes`.
- Mutation: `vgmlpro_source` removed from `vergeml_list_our_columns()`
  (`core/media-list.php:52`), `compat-free --tree` → `15/16`, `FAIL the free
  media list names vgmlpro_source as one of ours`; the seat still went back.
- MySQL: Pro 1.0.2 installed on `upg` through `wp plugin install --force`
  (sha256 read on the box first), **`23/23 passed`**; `upg` after: 20
  attachments, 0 probe files, `vergeml_ai` as found, key option absent, Pro
  `inactive 1.0.2`, **0 Pro lines in `debug.log`** across install, activation,
  the screens, the suite and deactivation.
- Screens (`docs/superpowers/mocks/shots/2026-09-20-pro-on-4-0-0-*.png`):
  licence page `200`, Connected, Single, 1 / 1, 2,000 credits (key masked by
  Pro's own `vgmlpro_mask_key`); media list `200`, Alt text column visible,
  **20 of 20 cells "We wrote this"**; attachment screen `200`, alt present.
- Pro's own suites on the box, **pro HEAD `a57bc77`** (sealed key at rest —
  not the customer's code; supporting evidence): api-base **2/3**,
  api-base-https 2/2, api-base-insecure 1/1, api-base-localhost 1/1, licence
  5/5, updates 4/4, seats 3/3, seats-staging 1/1, seats-deactivate 2/2,
  compat-free 17/17 (that run's count). The api-base miss is
  `/var/www/wp/wp-config.php:101` — `VERGEML_AI_SERVICE = http://127.0.0.1:3100/v1`
  (a `next-server` on the box) — the box's configuration, not Pro's code.
- Two review passes: 21 + 22 findings triaged in the spec's log; 19 patched
  (commits `c84ecef` `81f6fd3` `ef1709b` in pro; `3416589` `a1a5a7c` `45a96a4`
  `7f1936e` in plugin), 4 deferred, the rest rejected with the refutation.

## Found, not done (on the record in `deferred-work.md`)

1. Pro's describe writes `_wp_attachment_image_alt` outside
   `vergeml_index_writing()`, so free locks `alt` for that picture —
   identical in 3.16.1, so not an update-day change; whether the lock is
   intended for Pro's own writes is a Pro question.
2. No `--free-tree` pairing (free working tree vs the Pro 1.0.2 archive) —
   the next free release's question, AD-6 binds it.
3. The 4.0.0 archive ships `.harness/active.json` (3,986 bytes): `.harness`
   is not `export-ignore`. A line for 4.0.1.
4. `api-base` on the box, above.
5. Pro 1.0.2 has no attachment-screen hook; provenance lives in the media
   list cell only. The epic's "attachment screen shows provenance" describes
   a screen Pro never had; the matrix's "attachment screen 200" holds.
6. `deploy.mjs --check`: the tech site holds another free build
   (`184e21aa924d` vs the tree's `522000d9d997`); not deployed this session.
7. `AGENTS.md` says 48 suites are registered; it is 49.

## Open, and Nathan's

- **Decided (spec row 30):** `compat-free-upg` is registered in the free
  battery only when `VGMLPRO_SEATS_KEY` is in the environment (one printed
  line when it is not; asked for by name without it → exit 2), and its
  entry carries `before: wp plugin activate …` and the runner's new `after:`
  field (`wp plugin deactivate …`, run whatever the suite exited with). A
  bare `node tools/verify.mjs` stays exit 0 without the key; with it, the
  suite runs from and returns to the fixture's documented state. Nathan
  also kept the licence-page shot with Pro's own masked key.
- **Epic 1 is done.** Every new install is 4.0.0, the channel's checks say
  so, a 3.16.1 site upgrades in place and keeps everything, Pro 1.0.2 works
  the morning after. `bmad-retrospective` on Epic 1 is optional.
- Still yours from S22: the 4.0.1 cut (FR10, FR12, the PHP 8.4+ fix, now also
  `.harness` export-ignore), Pro 1.0.3 and the minimum-free gate, the
  wordpress.org form. The rollback target is decided (retro, 2026-09-20):
  the served 3.16.1's code re-cut without the leaked files -- A-2 of the
  Epic 1 retro cuts it, proves it and walks from it.

## Next — S24

Epic 2 starts with story 2.1, the buyer walk, a real payment on your go with
the amount said first. Opener, cwd `plugin`:

```
Read docs/handoffs/2026-09-20-s23-pro-1-0-2-works-on-4-0-0.md, then
_bmad-output/planning-artifacts/epics.md (Epic 2, story 2.1) and
_bmad-output/implementation-artifacts/sprint-status.yaml. AGENTS.md loads
via CLAUDE.md. State which model you are and follow that profile in
~/.claude/harness/model-profiles.md. This session is bmad-build on story
2.1 (bmad-spec first: the buyer walk on 4.0.0 needs its spec). Stop points:
the real payment starts only on my go with the amount said; nothing on ms2
or the real shop. End with a handoff in docs/handoffs/.
```
