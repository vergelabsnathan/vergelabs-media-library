# Wave 1 — Story F: the 4.0.1 changelog and readme copy proposals

**Date:** 2026-09-20. **Model:** Sonnet 5, per
`plans/finish-the-suite.md`'s model table (F is the Sonnet shape).
**Worktree:** `wt/plugin-F`, branch `story/f-401-copy`. **From:**
`plans/finish-the-suite.md` (wave 1, story F) and
`docs/handoffs/2026-09-20-s25-buyer-walk-on-4-0-0.md`.

## What this session did

Read-only research and one new file. No code touched, `readme.txt`
untouched, nothing on any server, nothing spent.

Produced `docs/release-notes-4.0.1-proposal.md`: a proposal file, marked
as a proposal at the top, containing:

- (a) three 4.0.1 changelog lines — the support ticket no longer sends
  the licence key (FR10, story A), the CSV export quotes
  formula-leading cells (FR12, story B), the PHP 8.4+ deprecation fix
  already in the tree (commit `6dc3223`) — each with its file:line or
  commit evidence.
- (b) a rewritten "External services" sentence for the support ticket
  describing the post-story-A behaviour (key's short form + site,
  never the full key), diffed against the current `readme.txt:183`
  sentence.
- (c) a recommendation that `Tested up to` and `Requires PHP` do **not**
  move for 4.0.1, with the version-matrix evidence, plus a side note
  (out of scope for this story) that the Description's separate "PHP
  7.4 through 8.3" prose line looks stale against the box's PHP 8.5.4.
- (d) a two-line "What changed" strapline in the existing changelog
  voice.

## Sources used

- `plans/finish-the-suite.md` (whole file, then mini-specs A, B and F
  closely)
- `readme.txt` (changelog section in full, "External services" section
  in full, header fields)
- `_bmad-output/implementation-artifacts/release-notes-4.0.0.md`
- `_bmad-output/implementation-artifacts/deferred-work.md` (rows citing
  FR10, FR12, the PHP 8.4 fix)
- `docs/handoffs/2026-09-20-s25-buyer-walk-on-4-0-0.md`
- `_bmad-output/planning-artifacts/epics.md` (FR10, FR12 definitions —
  not in the story's required-reading list, but needed to quote FR10/
  FR12 correctly rather than paraphrase them)
- `docs/outbound-audit-2026-09-19.md` (confirms the ticket currently
  sends the plaintext key — background for why story A exists)
- `docs/wordpress-org-submission.md`, `docs/benchmarks.md`, and a grep
  across `docs/` for WordPress/PHP version mentions, for (c)
- `core/get-help.php:190-238` and `core/import-csv.php:130-196` (read
  directly, to cite the current, pre-story-A/B code as the "gap" the
  proposed lines describe — confirmed neither story's code has landed
  in this worktree)
- `git log --oneline 6dc3223 -1` and `git show 6dc3223 --stat` / `git
  show 6dc3223 -- core/ai.php` for the PHP 8.4+ fix's exact diff

## Could not source

- The exact shape (prefix vs. suffix) of the licence-key fragment story
  A will send in the ticket — the plan's own wording is internally
  inconsistent (calls it a "prefix" but shows a trailing shape,
  `…93RJ`). Flagged in the proposal file's "Unsourced" section rather
  than guessed.
- Any confirmation from story A or B's own worktrees, since this
  story's Files line is the proposal file alone and the two behaviours
  are, by the plan's own account, in progress elsewhere.

## Not done, on purpose

- `readme.txt` was not edited — the mini-spec forbids it here; that is
  wave 2's job, from Nathan's copy call on this proposal.
- No proof/test — story F has none (copy only).
