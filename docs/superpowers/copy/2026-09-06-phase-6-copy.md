# Phase 6 copy — the look-alike cards, 2026-09-06

The strings the Duplicates screen's second list carried, each with the rule in
spec §5 it failed, and what stands there now. `tests/tree/copy.mjs` asserts
every struck string is gone from `core/` and `js/`, and that the replacements
named at the foot are present.

| struck | rule it failed | now |
|---|---|---|
| Possibly related | a hedge for a heading; no number | `Look-alikes · 22 sets` |
| These look similar; we are not confident they are the same picture. Nothing here has a delete button, by design. | a note explaining the list instead of stating what it is and what can be done; "we" | three facts with the brand-mark bullet: `Alike to the scan, not the same file · only you can tell` / `Keep this one sets the other aside and rewrites its pages to the one kept` / `Set aside is not deleted · on disk and at its address for 30 days, taken back with one press` |
| Nothing else looked similar. | kept in shape, one word: the list is named look-alikes | `Nothing else looked alike.` |
| Open in the library ↗ | the one control of a set, opening only its first file | `Open both ↗` / `Open all 4 ↗`, one tab per file; a browser that allows one tab per click leaves `Open Slide2.jpg ↗` for the second press |
| freed by keeping one of each | false once Keep this one sets aside instead of deleting: nothing is freed until a person deletes after 30 days | `held by the extra copies` |
| 2 files · keep one and get 88.9 KB back (on a look-alike set) | a set line that promised a delete this list did not offer | the card: the pictures side by side, under each `1,925 × 700 · 88.9 KB` / `28 February 2014` / `No folder` / `Not used in a post, page, layout or widget`. The line stays on exact-duplicate sets, where the delete is offered, so it is not a struck string in `copy.mjs` |

## New, on the standard

| string | where | what it states |
|---|---|---|
| `Keep this one · Slide2.jpg set aside` | each side's button | the action and its consequence; on a set of four, `Keep this one · 3 set aside` |
| `Keep this one · 1 page rewritten, Slide2.jpg set aside` | the button when the other is used | the pages it rewrites, then what goes aside |
| `Keep this one · usage not scanned` | the button, disabled, without the usage scan | why it cannot be pressed, in its own label (§4.1) |
| `Usage not scanned · which pages show a picture decides what Keep this one rewrites` + `Scan usage · posts, pages, layouts, widgets` | above the cards, without the usage scan | the fact, and the control that changes it, with what it reads |
| `Keep both · not shown again` / `Keep all 4 · not shown again` | the card's foot | the action and its consequence |
| `Used in 1 place · Phase 6 walk POST` | a side's fourth fact, when used | the count, the page, linked, with its type |
| `01.jpg kept · Slide2.jpg set aside for 30 days · Undo until tomorrow 10:14 · Set aside ↓` | the line where the card was | done as one line where the person was looking (§4.3); the two things to press next |
| `Slide3.jpg kept · Phase 6 walk now shows it · slide02-700px.jpg set aside for 30 days` | the same line, a page rewritten | one or two pages by title; three or more as `3 pages now show it` |
| `Slide3.jpg kept · slide02-700px.jpg stays: 1 use in Site settings could not be rewritten` | the line when a use cannot be rewritten | the rule made visible: nothing set aside while in use unless the use is rewritten |
| `Both kept · not shown again · Undo` | after Keep both | |
| `Undone · Slide2.jpg back in the library · 1 page as it was` | after Undo | what came back; a page edited since is `left as it is` and said so |
| `Look-alike of 01.jpg, kept on 6 September 2026` | the reason on the set-aside file, read on the Set aside list | why it is there, for the person reading it in six weeks |
