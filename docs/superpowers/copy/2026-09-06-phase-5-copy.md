# Phase 5 copy — the AI screen, 2026-09-06

The strings the AI screen carried before the three tabs, each with the rule
in spec §5 it failed, and what stands there now. `tests/tree/copy.mjs`
asserts every struck string is gone from `core/` and `js/`, and that the
replacements named at the foot are present.

| struck | rule it failed | now |
|---|---|---|
| Descriptions, alt text and search, written from your images. | a lede that decorates; "images" | the facts line: `641 pictures · 641 described · 641 with alt text · last run 4 September 13:52` |
| Loading… | a placeholder where the server had the number | the facts line is printed by the server |
| Describe your images | "image" where the plugin says "picture" (dashboard to-do titles) | Describe your pictures |
| Describe your images once; search finds what pictures show… | the same, on the plugin's own menu line | Describe your pictures once; search finds what they show… |
| Each image is shown to the model once. The description powers search, alt text, and everything after it. | a lede under a kicker; a claim without a number | the three facts: `0 new pictures · 0 without alt text · 641 of 641 on the brief in use` |
| Describe new images | a button without its number | `Describe 24 new pictures`, or `Describe · nothing new` (disabled) |
| Fix missing alt text | a button without its number; "fix" | `Alt text for 12`, or `Alt text · none missing` (disabled) |
| Fix alt text on your SEO pages (3) | the number as an aside | `Alt text on your SEO pages · 3` |
| Run in the background — you can close this tab | "you can"; the fact is the tab can be closed | `In the background · this tab can be closed` |
| Done. / Stopped — the remaining files kept failing. / N to go | states without their numbers | `24 pictures described · 0 failed · 14:20`; `9 of 24 described · 15 left · Stop at 14:12`; progress in the button: `Describing 9 of 24` |
| Not running. / Finished. / Stopped. | the background run's states, same fault | `''`; `%d pictures described · %d failed`; `%d of %d described · %d left · stopped` |
| How it behaves · Set once — it applies to every run above. | a settings card explaining itself | gone: the brief is a conversation, the two switches sit where what they change is explained |
| What this site is about · Your trade's words, so descriptions name what you actually sell. Applies from now on. | a textarea nobody knew what to type into | the How it describes tab |
| Search · Media search also matches AI captions and tags. | "AI captions" | `Search also matches the caption, tags and title the model wrote` + `Saves as it is changed · applies to the next search` |
| Page context · Descriptions know which page an image is on — its title, and with Yoast… The model still describes only what it sees. | "image"; a reassurance as a second sentence | `Page context: the title of the page a picture is on and, with Yoast, Rank Math, SEOPress or AIOSEO, its focus keyphrase` + `Saves as it is changed · applies to the next picture described` |
| Save changes · Saves everything on this page. | a save bar beside a brief with its own control | the switches save on change (decision recorded in spec §12) |
| Credits · left · one credit describes one image | "image" | `One describes one picture` · `641 to describe the library again` |
| credits left · one credit describes one image (the rail, every screen) | "image" | `credits · one describes one picture` |
| one credit describes one image. (Licence screen) | "image" | `one describes one picture.` |
| Images go to the service only to be described; nothing else leaves your site. Captions and tags are stored in your own database. | two facts in one sentence; "images" | three facts: `Each picture goes to the service once, to be described` · `With it: the file name, title, caption, the page it is on, and the brief` · `Nothing else · the catalogue stays in your database` |

Kept, and asserted present: `Describe · nothing new` (core/ai-screen.php),
`On the picture itself: its alt text, only when empty` (core/ai-screen.php),
`Nothing is written until the brief is used` (core/brief.php),
`one describes one picture` (core/admin-shell.php).

Out of scope, left as found: `Finished. Refreshing the numbers…`
(core/journey.php, the dashboard's own run line); `core/help.php`'s per-control
help text, as in Phase 4.
